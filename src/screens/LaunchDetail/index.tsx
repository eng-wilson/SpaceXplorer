import React from "react";

import { SafeContainer, Container } from "./styles";

const LaunchDetail: React.FC = () => {
  return (
    <SafeContainer>
      <Container />
    </SafeContainer>
  );
};

export default LaunchDetail;
